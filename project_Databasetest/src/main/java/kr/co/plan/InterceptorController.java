package kr.co.plan;

import javax.servlet.http.HttpSession;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class InterceptorController {
	/*
	 * @GetMapping("/interceptor/login") public String login() { return
	 * "interceptor/login"; }
	 * 
	 * @GetMapping("/interceptor/logout") public String logout(HttpSession session)
	 * { session.invalidate(); return "redirect:home"; }
	 * 
	 * @GetMapping("allPlan") public String allPlan() { return "allPlan"; }
	 * 
	 * @GetMapping("inserttest") public String insert() { return "inserttest"; }
	 */	
}
