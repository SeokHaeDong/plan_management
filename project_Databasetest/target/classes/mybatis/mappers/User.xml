<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="user">
  	<insert id="UserJoin" parameterType="User_Info">
  		insert into User(user_email, user_pw, user_nick, user_birthday, user_gender) values (#{user_email}, #{user_pw}, #{user_nick}, #{user_birthday}, #{user_gender});
  	</insert>
  	
  	<select id="emailselect" resultType="String">
  		select user_email from User 
  	</select>
  	
  	<select id="nickselect" resultType="String">
  		select user_nick from User where user_nick = #{user_nick}
  	</select>
  	
  	<select id="userlongin" resultType="String">
  	 	select user_email, user_pw, user_nick from User
  	</select>
  	
  	
  </mapper>